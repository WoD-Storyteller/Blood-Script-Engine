{
  "name": "blood-script-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Blood Script Engine – Autonomous Storyteller system for World of Darkness (VTM v5)",
  "license": "UNLICENSED",

  "engines": {
    "node": ">=20.0.0"
  },

  "scripts": {
    "start": "node dist/main.js",
    "start:dev": "nest start --watch",
    "build": "nest build",

    "db:migrate": "knex migrate:latest",
    "db:rollback": "knex migrate:rollback",
    "db:status": "knex migrate:status",
    "db:reset": "knex migrate:rollback --all && knex migrate:latest",

    "lint": "eslint \"src/**/*.ts\"",
    "format": "prettier --write \"src/**/*.ts\""
  },

  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/config": "^3.1.1",
    "@nestjs/platform-express": "^10.3.0",

    "discord.js": "^14.15.3",

    "pg": "^8.11.5",
    "knex": "^3.1.0",

    "dotenv": "^16.4.5"
  },

  "devDependencies": {
    "@nestjs/cli": "^10.3.2",
    "@nestjs/schematics": "^10.1.2",
    "@nestjs/testing": "^10.3.0",

    "@types/node": "^20.11.30",

    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",

    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
